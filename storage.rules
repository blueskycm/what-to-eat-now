rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // 只要是登入的使用者即可（臨時用）
    match /theme/{path=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
